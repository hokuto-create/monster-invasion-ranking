# 魔物襲来ランキング — 作業メモ

クラン別スコアの日次ランキングを記録・表示する静的サイト。

## ファイル構成

- `data.json` — 全データ。`timestamps`(日付→集計時刻の文字列) と `entries`(1行1クラン)
- `index.html` — 単一ファイルのビューア。`CLAN_ALIASES` に改名の系譜、`CLAN_MERGES` に合併の系譜、`PARTIAL_DATES` に未集計サーバーを持つ
  - **`index.html` を変更したら `APP_VERSION` も必ず更新する**（ホーム画面アプリが新バージョンを検知して自動リロードする仕組み）
- `officers.json` / `clan-officers/` — クラン幹部の名簿スナップショット（合併・移籍の追跡用）

## 日次データの追加手順

1. チャットに貼られた `{"date","server","clan","score"}` の行を `data.json` の `entries` 末尾に追記する
2. 同じ日付を `timestamps` にも追加する（例: `"2026-07-26": "7/27 8:58集計"`）
3. 追記後に JSON をパースして検証し、件数・サーバー内訳・重複 (日付, サーバー, クラン) を確認する
4. スコアの合理性も最初に確認する
   - 各サーバー帯のブロック内でスコアが降順になっていること（貼り付けはランキング順のはずなので、崩れていたら転記ミスの疑い）
   - 各クランの直近の出場スコアと比較し、極端な変動（目安: 前回比 5倍超 または 1/5未満）があれば桁間違い・別クランの可能性としてユーザーに提示する
   - スコアが正の整数であること
   - 疑わしい値を見つけても自己判断で修正せず、貼られた値のまま提示してユーザーに確認する
5. スコアや日付は貼られた値をそのまま使う。丸めや補正はしない

## サーバー区分（SV帯）

2026-08-02 のサーバー統合で SV帯が 9区分 → 4区分（`1-30` / `31-100` / `101-140` / `141-186`）になった。

- `index.html` の `SV_MERGE_DATE` を境に、`svsFor(date)` が旧区分 `OLD_SVS`・新区分 `NEW_SVS` を返す
- 統合前のデータは旧区分の表記のまま変更しない。統合日以降のデータは新区分の表記で追記する
- 統合後に最初のデータを追加する際、実際の切替日が 2026-08-02 でなければ `SV_MERGE_DATE` を修正する

## 改名の扱い（重要）

**改名の可能性があるクランを見つけた場合は、必ずユーザーに確認を取ってから対応する。**
`CLAN_ALIASES` への登録も、前日の表記への統一も、自己判断で行わない。

確認の際は、以下を調べたうえで候補と根拠を提示する。

- 前日にいて当日消えたクラン / 当日現れたクランをサーバー別に突き合わせる
- 結合文字・旧字体・全角半角を除去して正規化した名前で全履歴と照合する（例: `參`/`参`、`X͓̽` の結合文字順）
- 候補ペアが同じ日に共存していないか（共存していれば別クラン、または合併の可能性）
- 両者のスコアと出場履歴の連続性

判断が付いた後の反映方法は2通りあり、どちらにするかもユーザーに確認する。

- 改名として扱う → `index.html` の `CLAN_ALIASES` に `"新名": ["旧名", ...古い順に全て]` を追加
- 表記ゆれとして扱う → 当日のデータを前日の表記に合わせ、エイリアスは追加しない

## 合併の扱い

複数クランが1つに合流した場合は `index.html` の `CLAN_MERGES` に登録する（これもユーザー確認必須）。

- 形式: `"合併後クラン": [{name: "合併元クラン", date: "合流日"}, ...古い順]`
- 合併元は**ランキングから消滅したのを確認してから**追加する。存続中は別クランなので登録しない
  （例: 月焔華 = あるてみす + IgnisFloris の合併だが、IgnisFloris は存続中のため未登録）
- 例外: 主力だけが移籍し、残留メンバーが同名クランで存続する場合は `{name, date, remains: true}` で登録する
  （例: 万鬼一華 → X͓̽ˣLiภk̶、2026-09-01）。合流日より前の記録は合併後クランの履歴に連結され、
  合流日以降の同名クランは**別系譜の新クラン**として扱われる。データの表記は変えない（同名のまま追記する）
- 改名系譜（`CLAN_ALIASES`）は合併元・合併後とも自動で辿られるので二重登録しない
- **合併は対等な立場で行われるものとして扱う**。画面上の表示・チャットでの説明とも「合併元」「〜へ合流」「吸収」のような
  上下関係を含む表現は使わず、「合併: ○○（日付合併）」「○○ と合併」のように対等な表現にする
  （`CLAN_MERGES` の `sources`/`target` はデータ構造上の内部名なので変更しない）
- 履歴表示・グラフ・期間平均では合併元の履歴が合併後クランに連結される。
  同日に合併元が複数いる日はベストスコアの1件を代表として扱う

## クラン名の文字の扱い

クラン名の特殊 Unicode（結合文字、`ﷻ`、旧字体など）は**変換せずそのまま**保存する。
ただし、見た目が同一で結合文字の順序だけが違う表記は同一クランとして扱い、
履歴で多数派の並び順に揃える（例: `U+0353` → `U+033D` の順）。

## Git

- 作業ブランチにコミットしてプッシュする。PR は明示的に依頼された時だけ作成する
